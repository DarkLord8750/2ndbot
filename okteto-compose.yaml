version: "3.10"
services:
  workers:
    build: .
    environment:
      - APP_ID=${APP_ID}
      - API_HASH=${API_HASH}
      - TG_BOT_TOKEN=${TG_BOT_TOKEN}
      - CHANNEL_ID=${CHANNEL_ID}
      - OWNER_ID=${OWNER_ID}
      - TG_BOT_WORKERS=${TG_BOT_WORKERS}
